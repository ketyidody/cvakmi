; Upload limits for the photo admin (bulk class-photo uploads).
; Per-file validation in the app caps images at 20MB; these give headroom
; for large batches uploaded in a single request.
upload_max_filesize = 25M
post_max_size = 1G
max_file_uploads = 300
memory_limit = 512M
max_execution_time = 300
